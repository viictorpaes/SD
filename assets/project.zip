Este é um arquivo placeholder para project.zip.
Substitua por um .zip real contendo o projeto. Para gerar no Windows PowerShell:
Compress-Archive -Path path\to\project\* -DestinationPath assets\project.zip -Force
